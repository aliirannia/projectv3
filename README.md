# 🚀 **Havirkesht - سیستم مدیریت کشاورزی هوشمند**

یک پنل مدیریت کامل و مدرن برای سیستم‌های کشاورزی هوشمند با طراحی **Responsive RTL** و پشتیبانی از **Dark/Light Mode**. این پروژه به‌طور کامل با **Vanilla JavaScript** و بدون هیچ‌گونه وابستگی به فریم‌ورک‌های خارجی توسعه داده شده است.

## 📋 **ویژگی‌های اصلی**

### 🔐 **صفحه ورود (Login)**
- **طراحی مدرن و ریسپانسیو** با پشتیبانی کامل از موبایل و تبلت
- **اعتبارسنجی Real-Time** با نمایش پیام‌های کاربرپسند
- **سیستم ذخیره توکن** با قابلیت Remember Me
- **تغییر تم Dark/Light** با ذخیره‌سازی در localStorage
- **پنل اطلاعات API** برای توسعه‌دهندگان
- **بررسی وضعیت آنلاین API** با نمایش وضعیت
- **افکت‌های انیمیشنی** و UI/UX روان

### 🎛️ **داشبورد مدیریت (Dashboard)**
- **سایدبار هوشمند** با منوی تعاملی
- **آمار زنده** (استان‌ها، شهرستان‌ها، روستاها، کاربران)
- **سیستم ناوبری** با Breadcrumb
- **جستجوی Real-Time** در جداول
- **پاکت‌بندی** (Pagination) پیشرفته
- **مودال‌های تعاملی** برای عملیات CRUD
- **اعلان‌ها و پیام‌های Toast**

### 🛠️ **ماژول‌های مدیریتی**
1. **🏙️ مدیریت استان‌ها** - ایجاد، مشاهده، حذف
2. **🏘️ مدیریت شهرستان‌ها** - با فیلتر بر اساس استان
3. **🏡 مدیریت روستاها** - با فیلتر بر اساس شهرستان
4. **👥 مدیریت کاربران** - ایجاد کاربر، ویرایش، تغییر وضعیت
5. **⚙️ تنظیمات سیستم** - تغییر رمز عبور، تنظیمات اعلان‌ها

## 🏗️ **ساختار پروژه**

```
havirkesht-dashboard/
│
├── 📄 index.html           # صفحه ورود
├── 📄 style.css           # استایل صفحه ورود
├── 📄 login-script.js     # منطق صفحه ورود
│
├── 📄 main.html           # صفحه داشبورد
├── 📄 main-style.css      # استایل داشبورد
├── 📄 main-script.js      # منطق داشبورد
│
└── 📁 assets/             # منابع (اختیاری)
```

## 🔧 **تکنولوژی‌های استفاده شده**

### **Frontend:**
- **HTML5** با ساختار معنایی
- **CSS3** با Custom Properties (CSS Variables)
- **Vanilla JavaScript** (بدون وابستگی به کتابخانه‌ها)
- **Font Awesome 6.4** برای آیکون‌ها
- **Vazirmatn Font** برای پشتیبانی فارسی

### **ویژگی‌های CSS:**
- **Flexbox & Grid** برای Layout
- **CSS Variables** برای مدیریت تم
- **Animations & Transitions** برای تجربه کاربری بهتر
- **Responsive Design** با Mobile-First Approach
- **RTL Support** کامل

### **JavaScript Features:**
- **Async/Await** برای مدیریت درخواست‌های API
- **Modular Code Structure** با Separation of Concerns
- **Error Handling** کامل
- **Debounce** برای جستجو
- **LocalStorage/SessionStorage** برای مدیریت حالت

## 🔗 **API Integration**

### **Endpoints:**
- `POST /token` - دریافت توکن دسترسی
- `POST /refresh-token` - تمدید توکن
- `POST /logout` - خروج از سیستم
- `POST /changepassword` - تغییر رمز عبور
- `GET /province/` - دریافت لیست استان‌ها
- `POST /province/` - ایجاد استان جدید
- `DELETE /province/{id}` - حذف استان
- `GET /city/` - دریافت لیست شهرستان‌ها
- `POST /city/` - ایجاد شهرستان جدید
- `GET /village/` - دریافت لیست روستاها
- `POST /village/` - ایجاد روستای جدید
- `GET /users/` - دریافت لیست کاربران
- `POST /users/admin/` - ایجاد کاربر توسط ادمین
- `DELETE /users/{id}` - حذف کاربر

## 📱 **پشتیبانی از دستگاه‌ها**

### **موبایل:**
- **iOS & Android** با UX بهینه
- **جلوگیری از زوم ناخواسته** در فیلدهای ورودی
- **افکت‌های لمسی** برای دکمه‌ها

### **تبلت:**
- **Layout تطبیقی** با سایدبار جمع‌شو
- **جدول‌های اسکرول‌شونده**

### **دسکتاپ:**
- **سایدبار ثابت** با منوی کامل
- **جداول با پاکت‌بندی** پیشرفته

## 🎨 **سیستم تم (Theme System)**

### **Light Theme:**
- رنگ اصلی: `#10b981` (سبز)
- رنگ ثانویه: `#6366f1` (بنفش)
- پس‌زمینه: `#f9fafb`
- متن: `#374151`

### **Dark Theme:**
- رنگ اصلی: `#34d399`
- رنگ ثانویه: `#818cf8`
- پس‌زمینه: `#111827`
- متن: `#f9fafb`

## 🔒 **امنیت و مدیریت حالت**

### **مدیریت توکن:**
- **ذخیره در localStorage** برای Remember Me
- **ذخیره در sessionStorage** برای حالت موقت
- **بررسی اعتبار توکن** قبل از انقضا
- **تمدید خودکار توکن** با refresh token

### **اعتبارسنجی:**
- **اعتبارسنجی سمت کلاینت** برای فرم‌ها
- **پیام‌های خطای فارسی** و کاربرپسند
- **بررسی وضعیت شبکه** و API

## 🚀 **راه‌اندازی و استفاده**

1. **کلون کردن پروژه:**
```bash
git clone https://github.com/username/havirkesht-dashboard.git
cd havirkesht-dashboard
```

2. **استفاده مستقیم:**
   - فایل `index.html` را در مرورگر باز کنید
   - یا روی سرور وب قرار دهید

3. **تنظیمات API:**
   - ویرایش متغیر `API_BASE_URL` در فایل‌های JavaScript
   - تنظیم `CLIENT_ID` و `CLIENT_SECRET`

## 📊 **کارایی و بهینه‌سازی**

### **Performance:**
- **Lazy Loading** برای تصاویر (آماده برای پیاده‌سازی)
- **Minified CSS/JS** (آماده برای production)
- **Debounced Search** برای کاهش درخواست‌ها

### **SEO & Accessibility:**
- **ساختار معنایی HTML**
- **برچسب‌های aria** برای دسترسی‌پذیری
- **تگ‌های متای** مناسب

## 🐛 **عیب‌یابی**

### **مشکلات رایج:**
1. **خطای CORS:** بررسی تنظیمات سرور API
2. **عدم اتصال:** بررسی متغیر `API_BASE_URL`
3. **مشکلات RTL:** اطمینان از پشتیبانی مرورگر

### **لاگ‌گیری:**
- **Console Logs** برای توسعه
- **پیام‌های کاربر** برای خطاها
- **بررسی وضعیت شبکه**

## 🔮 **قابلیت‌های آتی**

### **در دست توسعه:**
- [ ] **چارت‌های آماری** برای گزارش‌ها
- [ ] **اکسپورت داده** به Excel/PDF
- [ ] **اعلان‌های Real-Time**
- [ ] **پشتیبانی از PWA**
- [ ] **کش کردن داده‌ها** (Caching)

### **بهبودها:**
- [ ] **Unit Tests** با Jest
- [ ] **Bundle Optimization** با Webpack
- [ ] **Internationalization** (i18n)

## 🤝 **مشارکت**

1. **Fork** ریپازیتوری
2. **Branch** جدید ایجاد کنید (`git checkout -b feature/AmazingFeature`)
3. **Commit** تغییرات (`git commit -m 'Add some AmazingFeature'`)
4. **Push** به Branch (`git push origin feature/AmazingFeature`)
5. **Pull Request** ایجاد کنید

.

## 👥 **تیم توسعه**

- **طراحی و توسعه:** Ali Irannia
- **API Backend:** Havirkesht Team


## 📞 **پشتیبانی**

- **وبسایت:** [aliirannia.ir](https://aliirannia.ir)
- **ایمیل:** [aliirannia@gmail.com](mailto:aliirannia@gmail.com)
- **تلفن:** ۰۹۱۶ ۷۱۶ ۲۰۰۷
- **ساعات کاری:** ۹ الی ۱۷

---

*
